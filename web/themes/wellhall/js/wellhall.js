/**
 * @file
 * WellHall behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.wellhall = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
